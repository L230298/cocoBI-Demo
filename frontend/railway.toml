# Railway 前端配置(忽略此文件 - 服务级别由 Railway UI 配置)

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run build && npx vite preview --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
