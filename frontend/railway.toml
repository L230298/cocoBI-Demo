# Railway 前端配置

[build]
builder = "NIXPACKS"
buildCommand = "echo 'Using pre-built dist/ from git' && ls dist/"

[deploy]
# Node.js 零依赖 server.js
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
