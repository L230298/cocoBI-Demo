# Railway 前端配置
# NIXPACKS 跑 npm run build(在 frontend/);build script 自己 cp 到 /app/dist

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
